# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'com.example.views' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'PureLayout'

  # Pods for com.example.views

  target 'com.example.viewsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'com.example.viewsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
